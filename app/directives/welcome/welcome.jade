gs-status-bar(ng-model='dm.state.connection')
div.container
  div.row

      div.welcome
        h1.welcome__title Choose Your Hero

  div.row
    div.col-md-3
      h3
      div.input-group.input-group-lg
        select(data-ng-model="dm.chosenHero" data-ng-options="hero.name for hero in dm.heroes | orderBy:'name'")
        button.btn.btn-primary.btn-xs(ng-click="dm.getCharacterById(dm.chosenHero.id);dm.getSeriesById(dm.chosenHero.id)") Select
        br
    div.col-md-3
      h3
        a(ng-href="{{dm.character.urls[0]}}"){{dm.character.name}}
      img(src='{{dm.character.thumbnail.path}}/standard_xlarge.jpg')
    div.col-md-4
      h1
      p{{dm.character.description}}
        span(ng-if='dm.character.description===""') OOPS! It seems Marvel has not deemed this hero worthy of a proper description in their API. 

  div.row
    div.col-md-3
      h3 "I never thought that Spider-Man would become the world wide icon that he is. I just hoped the books would sell and I'd keep my job." 
         br
         |-STAN LEE

    div.col-md-7
      div(ng-show="dm.covers.length===0")
        h2 Something cool is coming, you just wait.
      div(ng-hide="dm.covers.length===0")
        h3
        h4 {{dm.character.name}} appeared in these series
      div.comic(ng-repeat="cover in dm.covers | orderBy:'startYear'")
       img(src='{{cover.img}}/portrait_xlarge.jpg')
       h5 {{cover.title}}
